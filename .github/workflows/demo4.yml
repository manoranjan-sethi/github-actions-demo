name: Multi Job Dependent Execution
on: workflow_dispatch

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - name: Build step
          run: echo "this is a build job"
    test:
        runs-on: ubuntu-latest
        needs: build
        steps:
        - name: Test step
          run: echo "this is a test job"
    deploy:
        runs-on: ubuntu-latest
        needs: test
        steps:
        - name: Deploy step
          run: echo "this is a deploy job"